{
  "name": "hauzral-pharmacy-pos-server",
  "version": "1.0.0",
  "description": "Hauzral Pharmacy POS - Cloudflare Workers Backend",
  "main": "src/worker.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create pharmacy-pos-db",
    "db:seed": "wrangler d1 execute pharmacy-pos-db --file=./scripts/seed.sql",
    "db:seed:local": "wrangler d1 execute pharmacy-pos-db --local --file=./scripts/seed.sql",
    "db:tables": "wrangler d1 execute pharmacy-pos-db --command \"SELECT name FROM sqlite_master WHERE type='table'\"",
    "db:tables:local": "wrangler d1 execute pharmacy-pos-db --local --command \"SELECT name FROM sqlite_master WHERE type='table'\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
